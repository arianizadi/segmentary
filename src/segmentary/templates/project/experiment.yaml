name: my_experiment
space: example
taxonomy_root: taxonomy
output_root: runs

stages:
  - name: train_my_data
    data:
      - name: my_dataset
        root: data
        loader: folder
        mapping: my_dataset
        train_split: train
        val_split: val
    init_from: pretrained
